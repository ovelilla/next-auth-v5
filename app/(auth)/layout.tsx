const AuthLayout = ({ children }: { children: React.ReactNode }): React.ReactElement => (
  <div className="flex sm:items-center justify-center h-full ">{children}</div>
);

export default AuthLayout;
