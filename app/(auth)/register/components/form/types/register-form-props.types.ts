export type RegisterFormPropsType = {
  buttonSubmit: {
    label: string;
  };
  fieldEmail: {
    label: string;
    placeholder: string;
  };
  fieldPassword: {
    label: string;
    placeholder: string;
  };
};
