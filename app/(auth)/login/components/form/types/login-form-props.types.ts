export type LoginFormPropsType = {
  buttonLinkForgotPassword: {
    href: string;
    label: string;
  };
  buttonSubmit: {
    label: string;
  };
  fieldEmail: {
    label: string;
    placeholder: string;
  };
  fieldPassword: {
    label: string;
    placeholder: string;
  };
};
