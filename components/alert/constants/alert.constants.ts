// Enumerations
import { AlertAlignment } from "../enumerations/alert.enumertions";

const constants = {
  ALIGN_DEFAULT: AlertAlignment.Center,
  FULL_WIDTH_DEFAULT: true,
};

export default constants;
